part of 'constants.dart';

class AppDurations {
  AppDurations._();

  // Micro-interactions and hover feedback
  static const Duration short1 = Duration(milliseconds: 100);
  static const Duration short2 = Duration(milliseconds: 150);

  // UI transitions and animations
  static const Duration medium1 = Duration(milliseconds: 200);
  static const Duration medium2 = Duration(milliseconds: 220);
  static const Duration medium3 = Duration(milliseconds: 250);
  static const Duration medium4 = Duration(milliseconds: 300);

  // Debounce and delayed effects
  static const Duration long1 = Duration(milliseconds: 350);
  static const Duration long2 = Duration(milliseconds: 500);
}
